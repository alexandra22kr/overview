import React from 'react';
import styled from 'styled-components';
import '../css/all.css';

const Footer = styled.footer`
    width: 100%;
    height: 10vh;
    background: linear-gradient(160deg, var(--yellow-color) 0%, var(--colar-color) 80%);
`;

export default Footer;